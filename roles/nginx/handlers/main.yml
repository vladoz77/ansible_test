---
# handlers file for nginx
- name: start nginx
  ansible.builtin.service:
    name: "nginx"
    state: started
    enabled: yes

- name: restart nginx
  ansible.builtin.service:
    name: "nginx"
    state: restarted
    enabled: yes

